<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>filter</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* dynamic CSS styles generated by EJS */
        <% for (var i=2; i <=vakken.length; i++) {
            %>ul li:nth-child(<%=i %>) {
                opacity: <%=(50 + (i - 1) * 2.5) / 100 %>;
            }

            <%
        }

        %>
    </style>
</head>

<body>
    <main>
        <section>
            <!--h1 is de titel-->
            <h1>Studs</h1>
            <!-- Filter toevoegen -->
            <p>Ufuk Argun,<span>Leerjaar 2</span></p>

            <form action="/filter" method="post">
                <button type="submit" name="jaar" value="1">Leerjaar 1</button>
                <button type="submit" name="jaar" value="2">Leerjaar 2</button>
                <button type="submit" name="jaar" value="3">Leerjaar 3</button>
            </form>

            <p>Kies de vakken waar je graag hulp bij wilt:</p>
        </section>


        <!-- gaat spelers zoeken -->

        <!-- Gevens uit database zichtbaar maken -->
        <form action="/nextPage" method="post">
            <ul>
                <% for(var i=0; i<vakken.length; i++) { %>
                    <li>
                        <label>
                            <input type="checkbox" name="selectedVakken" value="<%= vakken[i] %>" style="display:none">
                            <%= vakken[i] %>

                        </label>
                    </li>
                    <% } %>
            </ul>
            <button type="submit" class="next">--></button>
        </form>
    </main>
    <script src="../scripts/script.js"></script>
</body>

</html>